
<% if @events.any? %>
<ul>
	<% @events.each do |e| %>
	<li>
		<%= link_to e.name, event_path(e) %>
	</li>
	<% end %>
</ul>
<% else %>
<p> There are no events 
	<%= link_to "New event", new_event_path %>
</p>
<% end %>